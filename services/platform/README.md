# Platform Service

Enterprise kernel service for reusable financial product definitions, rule evaluation, configurable workflows, and domain event outbox publishing.

This service is intentionally product-agnostic so lending, deposits, gold loan, forex, HRMS, procurement, and future modules can share configuration instead of hardcoding approval and eligibility logic.
