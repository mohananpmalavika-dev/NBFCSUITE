openapi: 3.0.3
info:
  title: Insurance Service API
  version: 0.1.0
paths:
  /policies:
    post:
      summary: Create an insurance policy
      responses:
        '200':
          description: Policy created
    get:
      summary: List tenant policies
      parameters:
        - $ref: '#/components/parameters/TenantId'
      responses:
        '200':
          description: Policies
  /policies/{policy_id}:
    get:
      summary: Get a tenant policy
      parameters:
        - $ref: '#/components/parameters/PolicyId'
        - $ref: '#/components/parameters/TenantId'
      responses:
        '200':
          description: Policy
  /policies/{policy_id}/premium-payments:
    post:
      summary: Collect a premium payment
      parameters:
        - $ref: '#/components/parameters/PolicyId'
      responses:
        '200':
          description: Premium payment posted
  /premium-payments:
    get:
      summary: List premium payments
      parameters:
        - $ref: '#/components/parameters/TenantId'
      responses:
        '200':
          description: Premium payments
  /policies/{policy_id}/claims:
    post:
      summary: Submit a claim against a policy
      parameters:
        - $ref: '#/components/parameters/PolicyId'
      responses:
        '200':
          description: Claim submitted
  /claims/{claim_id}/decision:
    post:
      summary: Approve, reject, or settle a claim
      parameters:
        - in: path
          name: claim_id
          required: true
          schema:
            type: string
        - $ref: '#/components/parameters/TenantId'
      responses:
        '200':
          description: Claim updated
  /claims:
    get:
      summary: List tenant claims
      parameters:
        - $ref: '#/components/parameters/TenantId'
      responses:
        '200':
          description: Claims
  /health:
    get:
      summary: Health check
      responses:
        '200':
          description: Healthy
components:
  parameters:
    TenantId:
      in: query
      name: tenant_id
      required: true
      schema:
        type: string
    PolicyId:
      in: path
      name: policy_id
      required: true
      schema:
        type: string
